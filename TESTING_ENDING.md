# Инструкция по тестированию экрана победы

## Быстрый способ (для разработчиков)

### Вариант 1: Через консоль браузера
1. Откройте игру: http://localhost:3000/
2. Войдите в игру (Login или Register)
3. Откройте консоль разработчика (F12)
4. Выполните команду:
```javascript
// Установить флаг победы напрямую
window.gameState = { ...window.gameState, isGameWon: true };
```

### Вариант 2: Временное изменение кода
В файле `App.tsx`, в функции `handleReleaseProject()`, временно замените:
```typescript
const isAGIProject = released.id === 'proj_agi';
```
на:
```typescript
const isAGIProject = true; // ВРЕМЕННО для тестирования
```

Теперь любой выпущенный проект покажет экран победы.

## Полный игровой путь (как задумано)

1. **Запустите игру**: http://localhost:3000/
2. **Создайте аккаунт** или войдите
3. **Накопите ресурсы**:
   - Пишите код (клик по кнопке Code)
   - Зарабатывайте деньги
   - Повышайте репутацию
4. **Откройте меню Projects** (иконка 📁)
5. **Найдите проект "Project Singularity (AGI)"**
   - Требования: 5000 строк кода
   - Сложность: 100
   - Тип: AI
6. **Начните разработку** (Start Project)
7. **Напишите 5000+ строк кода**
8. **Выпустите проект** (Release Project)
9. **Экран победы появится автоматически!**

## Что должно произойти

✅ Экран победы появляется поверх всего интерфейса  
✅ Золотой текст "СИНГУЛЯРНОСТЬ ДОСТИГНУТА"  
✅ Показывается финальная статистика  
✅ Кнопка "ПЕРЕЗАГРУЗИТЬ СИСТЕМУ" работает  
✅ После рестарта игра начинается заново  

## Проверка сохранения

1. Выпустите AGI проект
2. Экран победы должен появиться
3. Обновите страницу (F5)
4. Экран победы должен остаться (состояние сохраняется)
5. Нажмите "ПЕРЕЗАГРУЗИТЬ СИСТЕМУ"
6. Игра начнется с нуля

## Возможные проблемы

### Проект AGI не появляется в списке
- Проверьте, что `constants.ts` содержит проект с `id: 'proj_agi'`
- Убедитесь, что сервер перезапущен после изменений

### Экран победы не появляется
- Откройте консоль (F12) и проверьте `gameState.isGameWon`
- Убедитесь, что в логах есть сообщение "СИНГУЛЯРНОСТЬ ДОСТИГНУТА"

### Ошибки TypeScript в IDE
- Это нормально, React загружается через CDN
- Игра работает корректно несмотря на предупреждения IDE
