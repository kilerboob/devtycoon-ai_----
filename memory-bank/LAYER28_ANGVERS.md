# LAYER 28: ANG Vers Social Corporation

**Статус:** ✅ Реализовано  
**Дата:** 29 Ноября 2025  
**Тип:** S-Tier Corporation (Социальная инфраструктура)

---

## 🌐 Обзор

**ANG Vers** — это корпорация-монолит социальной инфраструктуры CyberNation. Это единственная **S-Tier** корпорация в игре, управляющая:

- 📱 Цифровыми профилями всех игроков
- 🔗 Социальными связями (друзья, враги, коллеги)
- ⭐ Рейтингами и репутацией
- 📝 Контрактами и наймом
- 🏛️ Гильдиями и студиями
- 💬 Коммуникациями (Messenger, DeVCall)
- 🛡️ Социальной безопасностью
- ⚖️ Арбитражем и жалобами

> **"ANG Vers владеет не железом, а людьми, информацией и связями — что делает её самой опасной корпорацией планеты."**

---

## 🏗️ Архитектура

### Новые типы (types.ts)

```typescript
// Типы социальных связей
type SocialConnectionType = 'friend' | 'colleague' | 'rival' | 'enemy' | 'blocked' | 'guild_member' | 'studio_partner' | 'contractor' | 'employer';

// Социальный профиль
interface SocialProfile {
  id: string;
  username: string;
  displayName: string;
  avatar: string;
  status: SocialStatus;
  socialRating: number; // 0-1000
  trustScore: number; // 0-100
  // ... полный интерфейс в types.ts
}

// Гильдия
interface Guild {
  id: string;
  name: string;
  tag: string;
  type: 'hacker' | 'security' | 'trader' | 'developer' | 'engineer' | 'mixed';
  license: 'basic' | 'standard' | 'premium' | 'elite';
  // ...
}

// Студия
interface Studio {
  id: string;
  name: string;
  specialization: 'games' | 'apps' | 'web' | 'ai' | 'security' | 'mixed';
  license: 'indie' | 'pro' | 'enterprise';
  // ...
}

// Контракт
interface SocialContract {
  id: string;
  type: 'hire' | 'freelance' | 'collaboration' | 'guild_job' | 'studio_project';
  status: 'open' | 'in_progress' | 'completed' | 'disputed' | 'cancelled';
  payment: number;
  escrowHeld: number;
  // ...
}

// Полное состояние ANG Vers для игрока
interface ANGVersState {
  profile: SocialProfile;
  connections: SocialConnection[];
  guild?: Guild;
  studio?: Studio;
  activeContracts: SocialContract[];
  notifications: SystemNotification[];
  activeThreats: ThreatLog[];
  socialInfluence: number;
  angVersStanding: 'hostile' | 'neutral' | 'friendly' | 'partner' | 'elite';
}
```

---

## 📁 Сервисы

### 1. angVersService.ts (~600 строк)

Главный сервис корпорации:

| Функция | Описание |
|---------|----------|
| `createProfile()` | Создание социального профиля |
| `updateSocialRating()` | Изменение рейтинга |
| `verifyProfile()` | Верификация (KYC) |
| `createConnection()` | Создание связи |
| `createGuild()` | Регистрация гильдии |
| `createStudio()` | Регистрация студии |
| `createContract()` | Создание контракта |
| `acceptContract()` | Принятие с эскроу |
| `completeContract()` | Завершение и выплата |
| `fileComplaint()` | Подача жалобы |
| `sendNotification()` | Системные уведомления |
| `logThreat()` | Регистрация угроз NPC |
| `calculateInfluence()` | Расчёт влияния |

### 2. socialGraphService.ts (~400 строк)

Управление социальным графом:

| Функция | Описание |
|---------|----------|
| `getFriends()` | Получить друзей |
| `getEnemies()` | Получить врагов |
| `isBlocked()` | Проверка блокировки |
| `getMutualFriends()` | Общие друзья |
| `calculateSocialDistance()` | Расстояние в графе |
| `calculateCentrality()` | Центральность в сети |
| `getInfluenceScore()` | Влиятельность |
| `getAlliedGuilds()` | Союзные гильдии |
| `getRivalGuilds()` | Гильдии-соперники |
| `recommendFriends()` | Рекомендации друзей |
| `generateNPCProfile()` | Генерация NPC |
| `generateNPCGuild()` | Генерация NPC гильдии |

### 3. socialMarketService.ts (~500 строк)

Социальные рынки:

| Функция | Описание |
|---------|----------|
| `createServiceListing()` | Листинг услуги |
| `searchListings()` | Поиск листингов |
| `getTopListings()` | Топ предложения |
| `createJobPosting()` | Открытый контракт |
| `searchJobs()` | Поиск работы |
| `assignContractor()` | Назначить исполнителя |
| `openDispute()` | Открыть спор |
| `resolveDispute()` | Разрешить спор |
| `calculateSellerReputation()` | Репутация продавца |
| `getCategoryStats()` | Статистика категории |
| `getMarketTrends()` | Рыночные тренды |

---

## 💰 Экономика ANG Vers

### Тарифы корпорации

```typescript
const ANG_VERS_FEES = {
  // Лицензии гильдий
  guildLicenseBasic: 500,
  guildLicenseStandard: 2000,
  guildLicensePremium: 10000,
  guildLicenseElite: 50000,
  
  // Лицензии студий
  studioLicenseIndie: 1000,
  studioLicensePro: 5000,
  studioLicenseEnterprise: 25000,
  
  // Комиссии
  contractEscrowFee: 0.05, // 5%
  marketListingFee: 50,
  featuredListingFee: 500,
  
  // Верификация
  verificationLevel1: 100,
  verificationLevel2: 500,
  verificationLevel3: 2000,
};
```

### Социальные рейтинги

| Рейтинг | Диапазон | Эффект |
|---------|----------|--------|
| Изгой | 0-99 | Блокировка большинства функций |
| Ненадёжный | 100-199 | Ограниченные контракты |
| Новичок | 200-349 | Базовый доступ |
| Участник | 350-499 | Полный доступ к рынку |
| Доверенный | 500-649 | Премиум возможности |
| Уважаемый | 650-799 | VIP статус |
| Влиятельный | 800-949 | Привилегии элиты |
| Элита | 950-1000 | Полная власть |

---

## 🎮 Геймплей

### Как игроки взаимодействуют с ANG Vers

1. **Социальный паспорт** — автоматически создаётся при регистрации
2. **Гильдии** — требуют лицензию ANG Vers для создания
3. **Студии** — регистрируются через корпорацию
4. **Контракты** — эскроу и арбитраж через ANG Vers
5. **Репутация** — все рейтинги хранятся в системе
6. **Угрозы** — NPC угрозы логируются для безопасности

### Социальные войны

Гильдии Hacker ↔ Security могут атаковать инфраструктуру ANG Vers:

- 📊 Манипуляция репутацией
- 🔓 Кража персональных данных
- ⚖️ Изменение рейтингов
- 🎭 Скрытие следов взломов

---

## 🔌 Интеграции

| Система | Интеграция через ANG Vers |
|---------|---------------------------|
| Messenger | Хранение переписок, угроз, системных уведомлений |
| DeVCall | Маршрутизация звонков |
| Social System | Фундаментальная корпорация |
| Contracts | Управление заказами и рейтингами |
| Hacking | Место атак и защиты |
| Blueprint Theft | Отслеживание последствий |
| NPC Threats | Централизованный лог угроз |
| Government | Выполнение цифровых приказов |

---

## 📊 Corporation Tiers

| Tier | Корпорация | Влияние | Описание |
|------|-----------|---------|----------|
| **S** | 🌐 ANG Vers | 100 | Социальная инфраструктура |
| **A** | ⚙️ TITAN Industries | 85 | CPU, AI-чипы |
| **A** | 🛰️ Orbitron SpaceWorks | 78 | Спутники, глобальная сеть |
| **B** | 💾 NovaTek Labs | 72 | SSD, RAM, хранение |
| **B** | 🔧 CyberForge Systems | 58 | Корпуса, охлаждение |
| **C** | ☠️ BlackSun Dynamics | 45 | Кибероружие (Shadow) |

---

## 📂 Файлы слоя

```
services/
├── angVersService.ts      # Главный сервис (~600 строк)
├── socialGraphService.ts  # Социальный граф (~400 строк)
├── socialMarketService.ts # Рынки услуг (~500 строк)
└── corporationService.ts  # Обновлён с ANG Vers

types.ts                   # +300 строк типов LAYER 28
constants.ts               # Добавлен angVersState
```

---

## ✅ Статус реализации

- [x] Типы для социальных профилей, связей, гильдий, студий
- [x] angVersService — управление профилями, контрактами, жалобами
- [x] socialGraphService — социальный граф, метрики, рекомендации
- [x] socialMarketService — рынок услуг, поиск работы
- [x] ANG Vers как S-Tier корпорация в corporationService
- [x] ANGVersState в GameState
- [x] Документация LAYER28_ANGVERS.md
- [ ] UI компоненты (будущие слои)

---

## 🚀 Следующие шаги

1. **UI для социального профиля** — отображение в Control Panel
2. **Гильдейское меню** — создание и управление
3. **Marketplace UI** — поиск услуг и контрактов
4. **Интеграция с Messenger** — угрозы и уведомления
5. **PvP социальные войны** — атаки на репутацию

---

*ANG Vers — Connecting the Digital World* 🌐
